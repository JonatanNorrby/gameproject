import { MIGRATION_STATUS } from '../core/config.js';

export const ECONOMY_SYSTEM_STATUS = MIGRATION_STATUS.SKELETON_CREATED;

// Future owner: crystal/ore production, truck logistics, storage/refining and export ships.
export function updateEconomy(_game, _dt) {
  // Prompt 2 interface only.
}
