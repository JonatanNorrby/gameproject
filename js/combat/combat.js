import { MIGRATION_STATUS } from '../core/config.js';

export const COMBAT_SYSTEM_STATUS = MIGRATION_STATUS.SKELETON_CREATED;

// Future owner: targeting coordination, damage, healing, repair and projectiles.
export function updateCombat(_game, _dt) {
  // Prompt 2 interface only.
}
