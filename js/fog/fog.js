import { MIGRATION_STATUS } from '../core/config.js';

export const FOG_SYSTEM_STATUS = MIGRATION_STATUS.SKELETON_CREATED;

// Future owner: explored state, vision sources and fog update data. Rendering of
// that data remains the renderer's responsibility.
export function updateFog(_game, _dt) {
  // Prompt 2 interface only.
}
